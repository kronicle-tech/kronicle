package tech.kronicle.service.scanners.gradle.GradleScannerCodebaseTest.DependencyNamedPartsWithClassifierAndExt

plugins {
    id "java"
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: "org.springframework.boot", name: "spring-boot-starter-web", version: "2.3.4.RELEASE", classifier: "anything", ext: "anything"
}
