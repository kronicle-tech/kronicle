package tech.kronicle.service.scanners.gradle.GradleScannerCodebaseTest.DependencyFileTree

plugins {
    id "groovy"
}

dependencies {
    compile fileTree(dir: "${rootDir}/libs", include: ["*.jar"])
}
