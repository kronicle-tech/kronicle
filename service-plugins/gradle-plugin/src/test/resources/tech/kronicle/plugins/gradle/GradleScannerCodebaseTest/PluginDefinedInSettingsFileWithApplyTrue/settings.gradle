package tech.kronicle.plugins.gradle.GradleScannerCodebaseTest.PluginDefinedInSettingsFileWithApplyTrue

pluginManagement {
    repositories {
        mavenCentral()
    }

    plugins {
        id "org.springframework.boot" version "$springBootVersion" apply true
    }
}
