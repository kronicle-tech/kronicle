package tech.kronicle.plugins.gradle.GradleScannerCodebaseTest.DependencyVariableInDependencies

plugins {
    id "java"
}

repositories {
    mavenCentral()
}

dependencies {
    def springBootVersion = "2.3.4.RELEASE"
    def hibernateValidatorVersion = "6.1.6.Final"

    compile "org.springframework.boot:spring-boot-starter-web:" + springBootVersion
    compile "org.hibernate.validator:hibernate-validator:$hibernateValidatorVersion"
}
