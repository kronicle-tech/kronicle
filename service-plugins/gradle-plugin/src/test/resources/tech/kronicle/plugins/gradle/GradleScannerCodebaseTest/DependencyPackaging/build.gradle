package tech.kronicle.plugins.gradle.GradleScannerCodebaseTest.DependencyPackaging

plugins {
    id "java"
}

repositories {
    mavenCentral()
}

dependencies {
    // The `sources` on the end of the dependency is the `packaging` of the dependency.
    compile "org.springframework.boot:spring-boot-starter-web:2.3.4.RELEASE:sources"
}
