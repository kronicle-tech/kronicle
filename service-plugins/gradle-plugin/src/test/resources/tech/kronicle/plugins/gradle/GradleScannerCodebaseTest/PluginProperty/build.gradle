package tech.kronicle.plugins.gradle.GradleScannerCodebaseTest.PluginProperty

buildscript {
    ext {
        springBootVersion = "2.3.4.RELEASE"
    }
}

repositories {
    mavenCentral()
}

plugins {
    id "org.springframework.boot" version "$springBootVersion"
}
