plugins {
    id "java"
}

repositories {
    mavenCentral()
}

dependencies {
    compile([group: "org.springframework.boot", name: "spring-boot-starter-web", version: "2.3.4.RELEASE"],
            [group: "org.springframework.boot", name: "spring-boot-starter-actuator", version: "2.3.4.RELEASE"])
}
