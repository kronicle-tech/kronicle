package tech.kronicle.service.scanners.gradle.GradleScannerTest.PluginProperty

buildscript {
    ext {
        springBootVersion = "2.3.4.RELEASE"
    }
}

repositories {
    mavenCentral()
}

plugins {
    id "org.springframework.boot" version "$springBootVersion"
}
